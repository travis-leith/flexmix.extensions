% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FLXPcategorical.R
\name{FLXPcategorical}
\alias{FLXPcategorical}
\title{Categorical-only concomitant model for flexmix}
\usage{
FLXPcategorical(formula = ~1)
}
\arguments{
\item{formula}{A one-sided formula of categorical concomitant regressors.
Defaults to \code{~1} (a single cell, equivalent to a constant prior).}
}
\value{
An object of class \code{FLXPcategorical}, to be passed as the
\code{concomitant} argument of \code{\link[flexmix:flexmix]{flexmix::flexmix()}}.
}
\description{
Concomitant model driver that estimates component priors as
the mean posterior responsibility within each \emph{cell} of a fully categorical
design. A cell is one distinct combination of the categorical regressor
values. This is the non-parametric analogue of \code{\link[flexmix:FLXPmultinom]{flexmix::FLXPmultinom()}}:
instead of a softmax of a linear predictor, the prior for an observation is
the average responsibility among observations sharing its covariate cell.
}
\details{
Because the model has no coefficient vector and no smooth likelihood, it
cannot participate in gradient-based inference. \code{existGradient()} returns
\code{FALSE}, so \code{refit(method = "optim")} is correctly refused. EM fitting,
prediction, simulation (\code{rflexmix}/\code{simulate}), relabelling, and
\code{refit(method = "mstep")} are all supported.

All regressors must be categorical. Numeric predictors trigger a clear
error; convert them to factors or use \code{\link[flexmix:FLXPmultinom]{flexmix::FLXPmultinom()}} instead.
}
\examples{
set.seed(1)
n <- 300
g <- factor(sample(c("a", "b", "c"), n, replace = TRUE))
y <- rnorm(n, mean = c(a = -2, b = 0, c = 2)[g])
df <- data.frame(y = y, g = g)
flexmix::flexmix(
  y ~ 1,
  data = df, k = 3,
  concomitant = FLXPcategorical(~g)
)

}
\seealso{
\code{\link[flexmix:FLXPmultinom]{flexmix::FLXPmultinom()}}
}
