% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fast_multinom.R
\name{FastFLXPmultinom}
\alias{FastFLXPmultinom}
\title{Fast multinomial concomitant model for flexmix}
\usage{
FastFLXPmultinom(
  formula = ~1,
  max_iter = 100L,
  tol = 1e-08,
  ridge = 1e-06,
  nthreads = 0L
)
}
\arguments{
\item{formula}{A one-sided formula of concomitant regressors.
Defaults to \code{~1}.}

\item{max_iter}{Integer specifying the maximum number of Newton-Raphson
iterations. Defaults to \code{100L}.}

\item{tol}{Numeric scalar defining convergence tolerance. Defaults to \code{1e-8}.}

\item{ridge}{Numeric scalar defining a ridge regularization penalty added to the
Hessian diagonal to prevent numerical instability or separation issues.
Defaults to \code{1e-6}.}

\item{nthreads}{Integer indicating the number of threads for parallel computation.
Defaults to \code{0L} (sequential execution).}
}
\value{
An object of class \code{FLXPmultinom} (subclass of \code{FLXP}) to be passed
as the \code{concomitant} argument to \code{\link[flexmix:flexmix]{flexmix::flexmix()}}.
}
\description{
Concomitant model driver that estimates component priors using a multinomial
logit model. It serves as a high-performance drop-in replacement for
\code{\link[flexmix:FLXPmultinom]{flexmix::FLXPmultinom()}}, leveraging a C++ Newton-Raphson solver for
accelerated parameter estimation.
}
\details{
Fits multinomial logit models for concomitant variables in mixture-of-experts
models. Because it constructs a \code{multinom}-compatible S4 object, downstream
methods like \code{summary()}, \code{vcov()}, and \code{refit(method = "optim")} continue
work seamlessly.
}
\examples{
set.seed(1)
n <- 500
z <- rnorm(n)
y <- rnorm(n, mean = ifelse(z > 0, 2, -2))
df <- data.frame(y = y, z = z)
flexmix::flexmix(
  y ~ 1,
  data = df, k = 2,
  concomitant = FastFLXPmultinom(~z)
)

}
\seealso{
\code{\link[flexmix:FLXPmultinom]{flexmix::FLXPmultinom()}}
}
